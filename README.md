# EyeFlowPython

Minimal Python project scaffold managed with `uv`.

## Quick Start

```powershell
uv sync
uv run python -m eyeflowpython
```

## Common Commands

```powershell
uv add <package>
uv add --dev pytest ruff
uv run eyeflowpython
uv lock
```
